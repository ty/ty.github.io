# hotlist Instructions

## Overview
This is a static web page where I can collect a list of links to other websites. It will have several sections. Each section may have a second level of subsections. Each section should be collapsable and draggable suc that the user can reorder the sections and subsections. Each link should open a new tab.

## Working Directory
**ALWAYS work from:** `/Users/tyler/clod/ty.github.io`

## Git Workflow - CRITICAL
**NEVER push commits to GitHub without explicit user confirmation.**

Before running `git push`:
1. Show me what commits will be pushed (`git log origin/main..HEAD`)
2. Ask: "Should I push these commits to GitHub?"
3. Wait for explicit "yes" confirmation
4. Only then execute `git push`

**Other Git Safety Rules:**
- Always commit with descriptive messages (not "fix", "update", etc.)
- Never force push (`git push -f`) without explicit permission
- Before merging branches, show me the diff and ask for confirmation
- Never delete branches without asking first
- Always run tests before committing if tests exist

## Important Reminders
- Check current directory with `pwd` before any file operations
- Always use absolute path: `/Users/tyler/clod/ty.github.io/`
